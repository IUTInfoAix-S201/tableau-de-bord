# data.json est genere a chaque run (deploye comme artefact Pages, pas versionne)
site/data.json

# artefacts de travail
__pycache__/
*.pyc
.DS_Store

# historique persiste via le cache Actions (genere localement aussi)
history/history.jsonl
